- hosts: localhost
  become: true
  pre_tasks:
    - name: pre-run | update package cache (ubuntu)
      tags: always
      apt: update_cache=yes
      changed_when: False
  tasks:
    - name: install apache
      tags: apache
      apt:
        - apache2
